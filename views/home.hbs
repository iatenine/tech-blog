{{#if loggedIn}}
{{!-- I really need to figure out how to do an if-not statement in handlebars --}}
{{>header}}
{{else}}
{{>login}}
{{/if}}

{{!-- Loop through posts --}}
{{#each posts}}
<div class="post container">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>{{this.updatedAt}}</p>
    {{!-- <p>{{this.comments}}</p> --}}
</div>
<hr />
{{/each}}